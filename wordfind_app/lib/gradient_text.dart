import 'package:flutter/material.dart';

class GradientText extends StatelessWidget {
  const GradientText(this.text, this.size, {super.key});

  @override
  Widget build(BuildContext context) {
    return  GradientText('text', 'size');
  }
}
